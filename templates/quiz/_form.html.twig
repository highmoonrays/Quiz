{{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('question_index', {'id': quiz.id}) }}">Add a question</a>
    <br>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
