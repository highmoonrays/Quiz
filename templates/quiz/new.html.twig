{% extends 'base.html.twig' %}

{% block title %}New Quiz{% endblock %}

{% block body %}
    {{ parent() }}
    <h1>Create new Quiz</h1>

    {{ include('quiz/_form.html.twig') }}
    <a href="{{ path('question_index') }}">Add a question</a>
    <a href="{{ path('quiz_index') }}">back to list</a>


{% endblock %}
